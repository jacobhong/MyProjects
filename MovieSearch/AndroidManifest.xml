<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moviesearch"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light" >
        <activity
            android:name="com.example.moviesearch.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.moviesearch.ViewMovies"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.example.moviesearch.VIEWMOVIES" />
            </intent-filter>
        </activity>
          <activity
            android:name="com.example.moviesearch.Bookmarks"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.example.moviesearch.BOOKMARKS" />
            </intent-filter>
        </activity>
    </application>

</manifest>